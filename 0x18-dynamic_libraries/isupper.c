#include "main.h"

/**
* _isupper - tests whether the letter is uppercase
* @c:character being tested
* Return: 1 if c is uppercase
* and 0 otherwise
*/
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
